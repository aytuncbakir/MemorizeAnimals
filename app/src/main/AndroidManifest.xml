<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sivamalabrothers.memorizeanimals">

    <uses-permission android:name="android.permission.INTERNET"/>
    
    <application
        android:allowBackup="true"
        android:icon="@mipmap/hayvanlar"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/hayvanlar"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".VeryEasyGame">

        </activity>

        <activity android:name=".EasyGame">

        </activity>

        <activity android:name=".NormalGame">

        </activity>

        <activity android:name=".HardGame">

        </activity>

        <activity android:name=".GirisSayfasi">

        </activity>

        <activity android:name=".AnimalsGameSplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>